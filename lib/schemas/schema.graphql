# import Course, CourseInput from './courses.graphql'

type Query {
  # Return all courses
  getCourses: [Course]

  # Return a single course
  getCourse(id: ID!): Course
}

type Mutation {
  createCourse(input: CourseInput!): Course
}
